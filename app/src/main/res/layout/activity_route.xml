<?xml version="1.0" encoding="utf-8"?>
<!--
    CollapsingToolbarLayout reemplazado por AppBarLayout + Toolbar estándar.
    Motivo: CollapsingToolbarLayout trunca títulos largos al colapsar.
    Con Toolbar estándar el título usa maxLines y ellipsize para mostrar
    el nombre completo de la ruta sin cortes.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.route.RouteActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/Theme.AguasDelRey.AppBarOverlay">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="?attr/actionBarSize"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            app:popupTheme="@style/Theme.AguasDelRey.PopupOverlay"
            app:titleTextAppearance="@style/ToolbarTitleMultiline" />

    </com.google.android.material.appbar.AppBarLayout>

    <include
        android:id="@+id/include"
        layout="@layout/content_scrolling" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>